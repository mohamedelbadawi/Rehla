import Hero from "@/components/custom/Hero";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <Hero />
      
    </div>
  );
};

export default Home;
